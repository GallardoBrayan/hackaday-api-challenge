<!DOCTYPE html>
<html>
  <head>
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body>
    <% if (dataType) { %>
    <div>
      <ul>
        <% for(var i=0; i < apiData.length; i++) { %>
        <li><%= apiData[i].name %></li>
        <% } %>
      </ul>
    </div>
    <% } %>
  </body>
</html>
